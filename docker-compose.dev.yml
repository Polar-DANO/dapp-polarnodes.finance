version: "3"
services:
  app:
    container_name: app
    command: npm run dev
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "7000:7000"
    volumes:
      - .:/app
      - /app/node_modules/
